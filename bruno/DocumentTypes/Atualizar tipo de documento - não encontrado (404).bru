meta {
  name: Atualizar tipo de documento - não encontrado (404)
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/document-types/00000000-0000-0000-0000-000000000000
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Não existe"
  }
}

vars:pre-request {
  baseUrl: localhost:3000
}

tests {
  test("Status 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
