meta {
  name: Criar tipo de documento - payload inválido (400)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/document-types
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": ""
  }
}

vars:pre-request {
  baseUrl: localhost:3000
}

tests {
  test("Status 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
