meta {
  name: Deletar colaborador
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/collaborators/{{collaboratorId}}
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: localhost:3000
}

tests {
  test("Status 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
