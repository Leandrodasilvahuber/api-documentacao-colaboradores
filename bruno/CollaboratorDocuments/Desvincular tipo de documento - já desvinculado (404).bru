meta {
  name: Desvincular tipo de documento - já desvinculado (404)
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/collaborators/{{collaboratorId}}/documents/{{documentTypeId}}
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: localhost:3000
}

tests {
  test("Status 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
